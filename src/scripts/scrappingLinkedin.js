// import { $ } from '../utils/selectors';
import { autoScroll } from '../utils/autoScroll';

const start = async () => {
    await autoScroll();
    console.log('termine de scrollear');
};


start();

